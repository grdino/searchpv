export default function SPVBranding() {
  return (
    <h1
      style={{
        fontWeight: 700,
        fontSize: "clamp(32px, 8vw, 56px)",
        lineHeight: 1,
      }}
    >
      SearchPV
    </h1>
  );
}